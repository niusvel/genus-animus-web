# URL de la API en PRODUCCIÓN.
# Vite carga este archivo en modo "production" (vite build, que es lo que ejecuta Vercel).
# Nota: una variable VITE_API_URL definida en el dashboard de Vercel tiene prioridad
# sobre este archivo. Para evitar confusiones, deja la URL solo en un sitio.
VITE_API_URL=https://genus-animus-api-production.up.railway.app
